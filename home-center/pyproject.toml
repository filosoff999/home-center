[project]
name = "home-center"
# Accepted predecessor baseline: version = "0.5.0"
version = "0.6.0"
description = "Local-first control plane for managed home infrastructure"
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.pyright]
pythonVersion = "3.12"
typeCheckingMode = "basic"
