[project]
name = "home-center"
# Historical accepted production baseline: version = "0.5.0"
# Accepted predecessor baseline: version = "0.6.0"
# Immediate predecessor candidate: version = "0.7.0"
# Published predecessor: version = "0.8.0"
# Immediate published predecessor: version = "0.9.2"
# Published 0.10 source baseline: version = "0.10.0"
# Immediate development predecessor: version = "0.11.0"
# Immediate development predecessor: version = "0.12.0"
version = "0.13.0"
description = "Local-first control plane for managed home infrastructure"
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.pyright]
pythonVersion = "3.12"
typeCheckingMode = "basic"

