# Home Center 0.18.0

Cumulative portable authorization/certificate/remote-access release. RBAC is provider-neutral and default-deny; certificate inventory is secret-free; remote access consumes runtime DNS/TLS/network/authentication readiness rather than embedded endpoints.
