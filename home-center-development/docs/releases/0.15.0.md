# Home Center 0.15.0

Cumulative infrastructure-neutral release with the verified portable runtime and
planning foundation, typed read-only infrastructure inventory, deterministic node
discovery and health aggregation, safe maintenance/drain preflight, and
arbitrary-topology deployment profiles.

Release qualification builds the Python wheel twice from the same source epoch,
requires byte-for-byte identical artifacts, verifies the `0.15.0` package/runtime
identity, checks required runtime data, runs the full unit suite, validates every
JSON contract, and enforces the infrastructure-neutrality boundary.
