# Home Center 0.63 — QR effect handoff and post-condition request

Status: runner-free source preparation. This document does not claim Release Candidate/Public Stable readiness and does not authorize product mutation.

## Observed authoritative boundary

- Public Stable: **0.62.1**.
- Next mandatory release train: **0.63.0**.
- Permitted non-runner planning horizon for this work: **0.63 → 0.64 → 0.65**.
- Canonical 0.63 foundation, deterministic artifact membership, terminal-state backup/restart recovery, receipt-only Audit, canonical StateStore migration, authenticated same-origin HTTP boundary and production V10 runtime/server wiring are already integrated through PR #250.
- This slice therefore starts from the first still-uncovered product boundary after those merged streams: typed effect handoff and post-condition verification preparation.

## Problem closed by this source slice

A successful QR `consume` or `revoke` proves only the one-time invitation state transition. It does **not** prove that guest access, a device binding, or access revocation was actually applied to authoritative product state. The existing runtime therefore correctly emits `onboarding_effect_verified=false` and names a separate typed Change/Job boundary.

This slice makes that next boundary explicit without executing it.

## Exact effect handoff

`QrOnboardingEffectHandoff` consumes only a typed terminal `QrOnboardingOperationReceipt` and its exact current `QrOnboardingRuntimeRecord`. It re-binds all of the following before it can produce a handoff:

- runtime record and invitation identity;
- invitation evidence SHA-256;
- exact Household snapshot/resource-version/generation;
- target member and subject;
- exact terminal record version and state;
- source receipt digest;
- the `next_required_boundary` already declared by the receipt.

Mappings are closed:

- consumed guest invitation → `typed-guest-access-change-job`;
- consumed device invitation → `typed-device-binding-change-job`;
- revoked invitation → `typed-access-revocation-change-job`.

Guest scope remains exactly the already-authorized bounded scope `internet.guest`; a transport/storage replay cannot broaden it to another capability. Device invitations remain tied to their exact registered device ID.

The handoff cannot grant effect execution, account creation, device registration, managed-state change, provider execution, infrastructure mutation or external publication. It states that an explicit typed Change/Job and later post-condition verification are still required.

## Verification request boundary

`QrOnboardingEffectVerificationRequest` may be created only after a concrete effect Job ID exists. It binds that Job to the exact handoff digest and requests one authoritative post-condition readback:

- guest access → `guest-access-effective`;
- device binding → `device-binding-effective`;
- revocation → `access-revoked`.

The request itself always carries:

- `authoritative_readback_required=true`;
- `post_condition_verified=false`;
- `effect_success_claimed=false`;
- provider/infrastructure/publication authority = false.

Transport reconstruction revalidates request/handoff identity, effect/job-type mapping, subject/device shape and deterministic request ID. Therefore creating or persisting the request can never turn a QR consume/revoke receipt into user-visible success.

## Artifact and contract boundary

Both runtime modules are added to deterministic wheel `REQUIRED_MEMBERS`. Closed Draft 2020-12 schemas reject unknown fields, prohibit guest-scope broadening and hard-code non-authorizing flags. Prepared tests cover guest/device/revocation mapping, exact-binding failures, authority-escalation/relabeling attempts, deterministic transport reconstruction, malformed digest/scope inputs and the no-false-success verification request.

No GitHub Actions workflow, check, rerun or workflow dispatch is part of this branch preparation. The work branch is deliberately outside the `main`/`release/**` push filters and remains without a pull request until a runner-enabled qualification stream deliberately picks it up.

## Still required before 0.63 RC/Public Stable

This source slice deliberately does not claim the following as complete:

1. durable creation/admission and execution of the actual typed guest-access/device-binding/revocation Change/Job using this handoff;
2. authoritative effect-specific product-state readback implementation and a **positive verification receipt only after readback succeeds**;
3. revalidation of current Household/member/device state immediately before any effect mutation;
4. recovery/idempotency behavior for an interrupted effect Job without treating an ambiguous outcome as success;
5. exact-head runner qualification of this prepared source after it is intentionally promoted into a runner stream;
6. final exact-candidate release identity, packaging, supported 0.62.1→0.63 install/upgrade/rollback, security/privacy gates and guarded Public Stable promotion.

The bounded technical Stable rule remains unchanged: unqualified HA/provider/commercial claims stay fail-closed/unclaimed rather than receiving invented evidence.
