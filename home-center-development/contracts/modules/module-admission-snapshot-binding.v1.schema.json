{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://home-center.local/contracts/modules/module-admission-snapshot-binding.v1.schema.json",
  "title": "Home Center module admission snapshot binding v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema",
    "binding_id",
    "snapshot_id",
    "home_center_version",
    "architecture",
    "operating_system",
    "available_capabilities",
    "installed_modules",
    "decisions",
    "installation_authorized",
    "execution_authorized",
    "production_mutation_enabled",
    "external_publication_authorized"
  ],
  "properties": {
    "schema": {
      "const": "home-center.module-admission-snapshot-binding.v1"
    },
    "binding_id": {
      "type": "string",
      "pattern": "^masb-[0-9a-f]{24}$"
    },
    "snapshot_id": {
      "type": "string",
      "pattern": "^mcs-[0-9a-f]{24}$"
    },
    "home_center_version": {
      "$ref": "#/$defs/semver"
    },
    "architecture": {
      "$ref": "#/$defs/symbolicId"
    },
    "operating_system": {
      "$ref": "#/$defs/symbolicId"
    },
    "available_capabilities": {
      "type": "array",
      "maxItems": 256,
      "uniqueItems": true,
      "items": {
        "$ref": "#/$defs/symbolicId"
      }
    },
    "installed_modules": {
      "$ref": "#/$defs/installedModules"
    },
    "decisions": {
      "type": "array",
      "minItems": 1,
      "maxItems": 256,
      "uniqueItems": true,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "module_id",
          "module_version",
          "decision_id",
          "status",
          "manifest_binding_sha256",
          "artifact_sha256"
        ],
        "properties": {
          "module_id": {
            "$ref": "#/$defs/moduleId"
          },
          "module_version": {
            "$ref": "#/$defs/semver"
          },
          "decision_id": {
            "type": "string",
            "pattern": "^madm-[0-9a-f]{24}$"
          },
          "status": {
            "enum": [
              "compatible",
              "blocked"
            ]
          },
          "manifest_binding_sha256": {
            "$ref": "#/$defs/digest"
          },
          "artifact_sha256": {
            "$ref": "#/$defs/digest"
          }
        }
      }
    },
    "installation_authorized": {
      "const": false
    },
    "execution_authorized": {
      "const": false
    },
    "production_mutation_enabled": {
      "const": false
    },
    "external_publication_authorized": {
      "const": false
    }
  },
  "$defs": {
    "semver": {
      "type": "string",
      "maxLength": 64,
      "pattern": "^(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)$"
    },
    "moduleId": {
      "type": "string",
      "maxLength": 128,
      "pattern": "^[a-z0-9](?:[a-z0-9.-]{0,126}[a-z0-9])?$"
    },
    "symbolicId": {
      "type": "string",
      "maxLength": 128,
      "pattern": "^[a-z][a-z0-9]*(?:[.-][a-z0-9]+)*$"
    },
    "digest": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "installedModules": {
      "type": "array",
      "maxItems": 256,
      "uniqueItems": true,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "version"
        ],
        "properties": {
          "id": {
            "$ref": "#/$defs/moduleId"
          },
          "version": {
            "$ref": "#/$defs/semver"
          }
        }
      }
    }
  }
}
