# Home Center contracts

These closed JSON Schema and OpenAPI documents define the public Home Center
0.14.1 request, response, configuration, planning, helper, module, health,
resource, backup, and release records. Consumers must reject unknown fields and
schema identities they do not support. Release acceptance and publication
schemas are version-neutral contracts; each record carries and validates its
own semantic product version.
